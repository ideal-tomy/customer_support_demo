import { CustomerSupportDemo } from "./components/CustomerSupportDemo";

/** Real AI path: Sample / BYOK / Trial + Retrieval + Structured Output. */
export default function App() {
  return <CustomerSupportDemo />;
}
